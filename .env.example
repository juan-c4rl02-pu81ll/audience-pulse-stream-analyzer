# Gemini API key used by the local Express backend.
# Optional for opening the app; required for real GenAI analysis.
# If omitted or left as the placeholder, the app uses the local freemium fallback.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Local app URL used for documentation and future callbacks.
APP_URL="http://localhost:3000"
